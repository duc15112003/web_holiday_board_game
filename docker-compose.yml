version: '3.8'

services:
  socket-server:
    build:
      context: ./socket-server
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      - CORS_ORIGIN=*
    restart: always

  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "81:80"
    restart: always
    depends_on:
      - socket-server
